apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-data.fullname" . }}-test-connection"
  labels:
    {{- include "demo-data.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-data.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
